<h1>Teds#index</h1>
<p>Find me in app/views/teds/index.html.erb</p>
<div class="container">
  <div class="row text-center filter_form">
    <% @teds.each do |ted| %>
      <p> <%= ted.name %> <%= link_to "details", ted_path(ted) %> <p>
      <% end %>
  </div>
  <!-- render 'filter_form' -->
</div>

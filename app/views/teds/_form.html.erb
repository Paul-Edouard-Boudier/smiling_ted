<%= simple_form_for([@user, @ted]) do |f| %>
  <%= f.input :name, label: t('name_creation')  %>
  <%= f.input :code, label: t('code') %>
  <h1>
    <%= f.button :submit, class: 'btn btn-primary' %>
  </h1>
<% end %>
